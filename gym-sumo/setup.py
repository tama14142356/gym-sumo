from setuptools import setup

setup(name='gym_sumo',
      version='0.0.1',
      # And any other dependencies foo needs
      install_requires=['gym', 'numpy', 'matplotlib', 'pillow'
                        'networkx', 'shapely', 'ipython']
      )
